@extends('layouts.admin')

@section('title', 'Kategori Produk')
@section('page-title', 'Kategori Produk')

@section('content')
    <div><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-xs font-bold uppercase tracking-[0.18em] text-rose-500">Katalog</p><h1 class="mt-2 font-serif text-3xl font-semibold tracking-tight text-stone-800">Kategori buket</h1><p class="mt-2 text-sm text-stone-500">Kelompokkan koleksi untuk memudahkan pelanggan menemukan buket.</p></div><a href="{{ route('admin.categories.create') }}" class="inline-flex h-11 items-center justify-center rounded-xl bg-rose-500 px-5 text-sm font-semibold text-white shadow-sm transition hover:bg-rose-600">+ Tambah kategori</a></div><div class="mt-7"><x-data-table :headers="['Kategori', 'Slug', 'Produk', 'Status', 'Aksi']" :is-empty="$categories->isEmpty()" empty-title="Belum ada kategori" empty-description="Mulai dengan membuat kategori untuk koleksi buket Anda.">@foreach($categories as $category)<tr><td class="px-5 py-4"><p class="font-semibold text-stone-700">{{ $category->name }}</p></td><td class="whitespace-nowrap px-5 py-4 text-sm text-stone-500">{{ $category->slug }}</td><td class="whitespace-nowrap px-5 py-4 text-sm text-stone-500">{{ $category->products_count }} produk</td><td class="whitespace-nowrap px-5 py-4"><x-status-badge :status="$category->is_active ? 'Aktif' : 'Nonaktif'" /></td><td class="whitespace-nowrap px-5 py-4"><div class="flex items-center gap-1"><a href="{{ route('admin.categories.edit', $category) }}" class="rounded-lg px-3 py-2 text-xs font-semibold text-rose-600 hover:bg-rose-50">Edit</a><form method="POST" action="{{ route('admin.categories.toggle', $category) }}">@csrf @method('PATCH')<button type="submit" class="rounded-lg px-3 py-2 text-xs font-semibold text-stone-500 hover:bg-stone-100">{{ $category->is_active ? 'Nonaktifkan' : 'Aktifkan' }}</button></form></div></td></tr>@endforeach</x-data-table></div><div class="mt-5">{{ $categories->links() }}</div></div>
@endsection
